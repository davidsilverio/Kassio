$(function() {
	const todoList = {
		$container: $(".todo-list"),
		$input : $("input:text"),
		$btn: $(".todo-list .btn"),
		
		init: function() {
			let tarefa = this.$input.on("keyup", (e) => {
				
			});
		}
	}
	
}